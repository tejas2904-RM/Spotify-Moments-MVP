services:
  - type: web
    name: spotify-moment-api
    runtime: node
    rootDir: spotify-moment/server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: CLIENT_URL
        sync: false
      - key: NODE_VERSION
        value: "20"
